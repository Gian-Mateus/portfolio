{
  "name": "site",
  "version": "1.0.0",
  "description": "Site de portifólio",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "watch": "npx tailwindcss -i ./public/assets/input.css -o ./public/assets/style.css --watch"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "install": "^0.13.0",
    "tailwindcss": "^3.4.1"
  },
  "dependencies": {
    "alpinejs": "^3.13.7"
  }
}
